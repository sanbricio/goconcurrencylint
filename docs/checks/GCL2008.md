# GCL2008 — add-zero

> wg.Add(0) is a no-op and usually means the intended count was lost.

|           |                              |
|-----------|------------------------------|
| Code      | `GCL2008` |
| Slug      | `add-zero` |
| Primitive | `sync.WaitGroup` |

## Why it matters

With a zero count Wait() returns immediately and the workers run unsynchronised.

## Examples

The linter flags code like this:

```go
var wg sync.WaitGroup
wg.Add(0) // no-op: Wait() returns immediately
go func() { defer wg.Done(); work() }()
wg.Wait()
```

Write it like this instead:

```go
var wg sync.WaitGroup
wg.Add(1)
go func() { defer wg.Done(); work() }()
wg.Wait()
```

## Suppressing this check

Add an inline directive on the offending line. Either the canonical code or the legacy slug is accepted:

```go
foo() // goconcurrencylint:ignore GCL2008
foo() // goconcurrencylint:ignore add-zero
```

See the [check catalogue](README.md) for every check.

<sub>Generated from the check registry by `scripts/gendocs` — do not edit by hand.</sub>
