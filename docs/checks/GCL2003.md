# GCL2003 — add-after-wait

> wg.Add() is called after wg.Wait() returned with an empty counter — a classic reuse bug.

|           |                              |
|-----------|------------------------------|
| Code      | `GCL2003` |
| Slug      | `add-after-wait` |
| Primitive | `sync.WaitGroup` |

## Why it matters

Adding to a group whose counter already hit zero races with the returned Wait().

## Examples

The linter flags code like this:

```go
wg.Wait()
wg.Add(1) // reusing a drained group races with the returned Wait()
go worker(&wg)
```

Write it like this instead:

```go
wg.Add(1)
go worker(&wg)
wg.Wait()
```

## Suppressing this check

Add an inline directive on the offending line. Either the canonical code or the legacy slug is accepted:

```go
foo() // goconcurrencylint:ignore GCL2003
foo() // goconcurrencylint:ignore add-after-wait
```

See the [check catalogue](README.md) for every check.

<sub>Generated from the check registry by `scripts/gendocs` — do not edit by hand.</sub>
