# GCL2010 — wait-without-add

> A local WaitGroup is waited on without any Add() in the same lifecycle.

|           |                              |
|-----------|------------------------------|
| Code      | `GCL2010` |
| Slug      | `wait-without-add` |
| Primitive | `sync.WaitGroup` |

## Why it matters

The Wait() is either a no-op or a sign that an Add() was lost, leaving the workers unsynchronised.

## Examples

The linter flags code like this:

```go
var wg sync.WaitGroup
go func() { work() }() // no Add/Done in this lifecycle
wg.Wait()              // nothing to wait for
```

Write it like this instead:

```go
var wg sync.WaitGroup
wg.Add(1)
go func() { defer wg.Done(); work() }()
wg.Wait()
```

## Suppressing this check

Add an inline directive on the offending line. Either the canonical code or the legacy slug is accepted:

```go
foo() // goconcurrencylint:ignore GCL2010
foo() // goconcurrencylint:ignore wait-without-add
```

See the [check catalogue](README.md) for every check.

<sub>Generated from the check registry by `scripts/gendocs` — do not edit by hand.</sub>
