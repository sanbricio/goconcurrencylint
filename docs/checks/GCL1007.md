# GCL1007 — defer-unlock-in-loop

> defer mu.Unlock() lives inside a loop body, so the unlock only runs at function return.

|           |                              |
|-----------|------------------------------|
| Code      | `GCL1007` |
| Slug      | `defer-unlock-in-loop` |
| Primitive | `sync.Mutex`, `sync.RWMutex` |

## Why it matters

The lock is held across every remaining iteration, serialising the loop or deadlocking it.

## Examples

The linter flags code like this:

```go
for _, it := range items {
	mu.Lock()
	defer mu.Unlock() // runs only when the function returns
	process(it)
}
```

Write it like this instead:

```go
for _, it := range items {
	mu.Lock()
	process(it)
	mu.Unlock()
}
```

## Suppressing this check

Add an inline directive on the offending line. Either the canonical code or the legacy slug is accepted:

```go
foo() // goconcurrencylint:ignore GCL1007
foo() // goconcurrencylint:ignore defer-unlock-in-loop
```

See the [check catalogue](README.md) for every check.

<sub>Generated from the check registry by `scripts/gendocs` — do not edit by hand.</sub>
