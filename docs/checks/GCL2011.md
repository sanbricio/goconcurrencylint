# GCL2011 — wait-deadlock

> Wait() is reached while the same goroutine still owes a Done().

|           |                              |
|-----------|------------------------------|
| Code      | `GCL2011` |
| Slug      | `wait-deadlock` |
| Primitive | `sync.WaitGroup` |

## Why it matters

The counter can never reach zero from this goroutine, so Wait() self-deadlocks.

## Examples

The linter flags code like this:

```go
var wg sync.WaitGroup
wg.Add(1)
wg.Wait() // this goroutine still owes the Done(): self-deadlock
wg.Done()
```

Write it like this instead:

```go
var wg sync.WaitGroup
wg.Add(1)
go func() { defer wg.Done(); work() }()
wg.Wait()
```

## Suppressing this check

Add an inline directive on the offending line. Either the canonical code or the legacy slug is accepted:

```go
foo() // goconcurrencylint:ignore GCL2011
foo() // goconcurrencylint:ignore wait-deadlock
```

See the [check catalogue](README.md) for every check.

<sub>Generated from the check registry by `scripts/gendocs` — do not edit by hand.</sub>
