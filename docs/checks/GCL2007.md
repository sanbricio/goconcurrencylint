# GCL2007 — add-loop-count-mismatch

> A literal Add(n) count does not match a statically countable loop of worker goroutines.

|           |                              |
|-----------|------------------------------|
| Code      | `GCL2007` |
| Slug      | `add-loop-count-mismatch` |
| Primitive | `sync.WaitGroup` |

## Why it matters

If Add(n) and the number of workers disagree, Wait() either returns early or blocks forever.

## Examples

The linter flags code like this:

```go
wg.Add(3) // count disagrees with the 5 workers started below
for i := 0; i < 5; i++ {
	go func() { defer wg.Done(); work() }()
}
wg.Wait()
```

Write it like this instead:

```go
const n = 5
wg.Add(n)
for i := 0; i < n; i++ {
	go func() { defer wg.Done(); work() }()
}
wg.Wait()
```

## Suppressing this check

Add an inline directive on the offending line. Either the canonical code or the legacy slug is accepted:

```go
foo() // goconcurrencylint:ignore GCL2007
foo() // goconcurrencylint:ignore add-loop-count-mismatch
```

See the [check catalogue](README.md) for every check.

<sub>Generated from the check registry by `scripts/gendocs` — do not edit by hand.</sub>
