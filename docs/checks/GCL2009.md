# GCL2009 — add-negative

> wg.Add(n) is called with a negative literal, which panics at runtime.

|           |                              |
|-----------|------------------------------|
| Code      | `GCL2009` |
| Slug      | `add-negative` |
| Primitive | `sync.WaitGroup` |

## Why it matters

A negative Add() drives the counter below zero and panics.

## Examples

The linter flags code like this:

```go
var wg sync.WaitGroup
wg.Add(-1) // a negative counter panics
```

Write it like this instead:

```go
var wg sync.WaitGroup
wg.Add(1)
defer wg.Done()
```

## Suppressing this check

Add an inline directive on the offending line. Either the canonical code or the legacy slug is accepted:

```go
foo() // goconcurrencylint:ignore GCL2009
foo() // goconcurrencylint:ignore add-negative
```

See the [check catalogue](README.md) for every check.

<sub>Generated from the check registry by `scripts/gendocs` — do not edit by hand.</sub>
