# GCL1005 — defer-lock

> defer mu.Lock()/RLock() is used where an unlock was almost certainly intended.

|           |                              |
|-----------|------------------------------|
| Code      | `GCL1005` |
| Slug      | `defer-lock` |
| Primitive | `sync.Mutex`, `sync.RWMutex` |

## Why it matters

Deferring Lock() acquires the lock at function return instead of releasing it — typically an immediate self-deadlock.

## Examples

The linter flags code like this:

```go
func work(mu *sync.Mutex) {
	mu.Lock()
	defer mu.Lock() // meant Unlock: re-locks at return
	// ...
}
```

Write it like this instead:

```go
func work(mu *sync.Mutex) {
	mu.Lock()
	defer mu.Unlock()
	// ...
}
```

## Suppressing this check

Add an inline directive on the offending line. Either the canonical code or the legacy slug is accepted:

```go
foo() // goconcurrencylint:ignore GCL1005
foo() // goconcurrencylint:ignore defer-lock
```

See the [check catalogue](README.md) for every check.

<sub>Generated from the check registry by `scripts/gendocs` — do not edit by hand.</sub>
